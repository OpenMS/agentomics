# ID Conflict Resolver

Resolve ambiguous peptide identification annotations on features, keeping only the best-scoring hit per feature.

## Usage

```bash
python id_conflict_resolver.py --input features.featureXML --output resolved.featureXML
```

## Dependencies

```
pyopenms
click
```
