# MS1 Feature Intensity Tracker

Track feature intensities across multiple mzML runs.

## Usage

```bash
python ms1_feature_intensity_tracker.py --inputs run1.mzML run2.mzML --features targets.tsv --ppm 10 --output tracking.tsv
```
