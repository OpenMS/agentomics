# Protein Group Reporter

Parse protein groups from peptide-level data and a FASTA database.

## Usage

```bash
python protein_group_reporter.py --input peptides.tsv --fasta db.fasta --output groups.tsv
```
